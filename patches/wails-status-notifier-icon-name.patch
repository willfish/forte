--- a/pkg/application/systemtray_linux.go
+++ b/pkg/application/systemtray_linux.go
@@ -9,6 +9,7 @@
 import (
 	"fmt"
 	"os"
+	"path/filepath"
 	"sync"
 	"sync/atomic"
 
@@ -25,6 +26,15 @@
 	menuPath = "/StatusNotifierMenu"
 )
 
+func forteStatusNotifierIconThemePath() string {
+	executable, err := os.Executable()
+	if err != nil {
+		return ""
+	}
+
+	return filepath.Clean(filepath.Join(filepath.Dir(executable), "..", "share", "icons"))
+}
+
 type linuxSystemTray struct {
 	parent *SystemTray
 
@@ -611,7 +621,7 @@
 			Callback: nil,
 		},
 		"IconThemePath": {
-			Value:    "",
+			Value:    forteStatusNotifierIconThemePath(),
 			Writable: false,
 			Emit:     prop.EmitTrue,
 			Callback: nil,
