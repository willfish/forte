# Implementation Plans

Generated by the improve skill on 2026-06-14 against commit `f32f673`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Align local verification commands with CI build tags | P1 | S | - | DONE |
| 002 | Ignore stale radio browse and search responses | P1 | M | 001 | DONE |
| 003 | Reconcile deleted local tracks during library scans | P2 | M | 001 | DONE |
| 004 | Add a supported local library scan workflow | P2 | L | 001, 003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 002 and 003 depend on 001 because they need a trustworthy local command to
  prove Go/frontend regressions.
- 004 depends on 003 because exposing local scans in the UI should not ship
  while full rescans can leave deleted files in the library.

## Findings considered and rejected

- Hardcoded external-service credentials: not found. The schema stores user
  server/scrobbling credentials in SQLite by design for a desktop app; no
  committed secret values were found during this audit.
- SQL injection in album sorting: rejected. `internal/library/albums.go`
  constructs ORDER BY strings, but the columns and direction are allow-listed
  in `directOrderClause` and `dedupOrderClause`.
- npm dependency advisories: rejected for now. `cd frontend && npm audit
  --audit-level=high --json` reported 0 total vulnerabilities.
