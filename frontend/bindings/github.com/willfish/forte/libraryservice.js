// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * LibraryService exposes the music library to the frontend.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * AddRadioFavourite saves a radio station to favourites.
 * @param {string} stationUUID
 * @param {string} name
 * @param {string} streamURL
 * @param {string} faviconURL
 * @param {string} tags
 * @returns {$CancellablePromise<void>}
 */
export function AddRadioFavourite(stationUUID, name, streamURL, faviconURL, tags) {
    return $Call.ByID(3744144887, stationUUID, name, streamURL, faviconURL, tags);
}

/**
 * AddServer adds a new streaming server configuration.
 * @param {$models.ServerConfig} cfg
 * @returns {$CancellablePromise<void>}
 */
export function AddServer(cfg) {
    return $Call.ByID(477958106, cfg);
}

/**
 * AddTrackToPlaylist adds a track to a playlist.
 * @param {number} playlistID
 * @param {number} trackID
 * @returns {$CancellablePromise<void>}
 */
export function AddTrackToPlaylist(playlistID, trackID) {
    return $Call.ByID(2287316659, playlistID, trackID);
}

/**
 * AlbumArtwork returns the artwork for an album as a base64 data URI.
 * @param {number} albumID
 * @returns {$CancellablePromise<string>}
 */
export function AlbumArtwork(albumID) {
    return $Call.ByID(866920135, albumID);
}

/**
 * CompleteLastFmAuth exchanges the authorized token for a session key.
 * @param {string} token
 * @returns {$CancellablePromise<void>}
 */
export function CompleteLastFmAuth(token) {
    return $Call.ByID(3698942302, token);
}

/**
 * ConnectListenBrainz validates the user token, retrieves the username, and saves
 * the configuration with scrobbling enabled.
 * @param {string} userToken
 * @returns {$CancellablePromise<void>}
 */
export function ConnectListenBrainz(userToken) {
    return $Call.ByID(1138196949, userToken);
}

/**
 * CreatePlaylist creates a new playlist and returns its ID.
 * @param {string} name
 * @returns {$CancellablePromise<number>}
 */
export function CreatePlaylist(name) {
    return $Call.ByID(4167498172, name);
}

/**
 * DeletePlaylist deletes a playlist.
 * @param {number} id
 * @returns {$CancellablePromise<void>}
 */
export function DeletePlaylist(id) {
    return $Call.ByID(2018893399, id);
}

/**
 * DeleteServer removes a streaming server configuration.
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function DeleteServer(id) {
    return $Call.ByID(3862467038, id);
}

/**
 * DisconnectLastFm clears the Last.fm session key and username.
 * @returns {$CancellablePromise<void>}
 */
export function DisconnectLastFm() {
    return $Call.ByID(970487533);
}

/**
 * DisconnectListenBrainz clears the ListenBrainz token and username.
 * @returns {$CancellablePromise<void>}
 */
export function DisconnectListenBrainz() {
    return $Call.ByID(902147985);
}

/**
 * GetAlbumTracks returns the tracks for a given album.
 * @param {number} albumID
 * @returns {$CancellablePromise<$models.AlbumTrack[]>}
 */
export function GetAlbumTracks(albumID) {
    return $Call.ByID(207489145, albumID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * GetAlbums returns all albums sorted by the given field and direction.
 * Source: "" (all, deduped), "local", "server".
 * @param {string} sort
 * @param {string} order
 * @param {string} source
 * @returns {$CancellablePromise<$models.Album[]>}
 */
export function GetAlbums(sort, order, source) {
    return $Call.ByID(1337880606, sort, order, source).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType3($result);
    }));
}

/**
 * GetArtistByName returns the artist ID for the given name.
 * @param {string} name
 * @returns {$CancellablePromise<number>}
 */
export function GetArtistByName(name) {
    return $Call.ByID(1148779767, name);
}

/**
 * GetArtistInfo returns metadata for the named artist, using cache with 30-day TTL.
 * @param {string} artistName
 * @returns {$CancellablePromise<$models.ArtistInfoJSON>}
 */
export function GetArtistInfo(artistName) {
    return $Call.ByID(2345670893, artistName).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType4($result);
    }));
}

/**
 * GetListenBrainzConfig returns the current ListenBrainz configuration.
 * @returns {$CancellablePromise<$models.ListenBrainzConfigJSON>}
 */
export function GetListenBrainzConfig() {
    return $Call.ByID(1867711289).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * GetPlaylistTracks returns the tracks in a playlist.
 * @param {number} playlistID
 * @returns {$CancellablePromise<$models.PlaylistTrack[]>}
 */
export function GetPlaylistTracks(playlistID) {
    return $Call.ByID(4244880336, playlistID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType7($result);
    }));
}

/**
 * GetPlaylists returns all playlists.
 * @returns {$CancellablePromise<$models.Playlist[]>}
 */
export function GetPlaylists() {
    return $Call.ByID(1524576557).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType9($result);
    }));
}

/**
 * GetRadioFavourites returns all saved radio stations.
 * @returns {$CancellablePromise<$models.RadioFavouriteJSON[]>}
 */
export function GetRadioFavourites() {
    return $Call.ByID(590575721).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType11($result);
    }));
}

/**
 * GetRadioStationsByCountry returns radio stations matching a country.
 * @param {string} country
 * @param {number} limit
 * @returns {$CancellablePromise<$models.RadioStationJSON[]>}
 */
export function GetRadioStationsByCountry(country, limit) {
    return $Call.ByID(3988982917, country, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType13($result);
    }));
}

/**
 * GetRadioStationsByTag returns radio stations matching a tag.
 * @param {string} tag
 * @param {number} limit
 * @returns {$CancellablePromise<$models.RadioStationJSON[]>}
 */
export function GetRadioStationsByTag(tag, limit) {
    return $Call.ByID(3897998615, tag, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType13($result);
    }));
}

/**
 * GetRecentlyPlayed returns the most recently played tracks.
 * @param {number} limit
 * @returns {$CancellablePromise<$models.RecentPlayJSON[]>}
 */
export function GetRecentlyPlayed(limit) {
    return $Call.ByID(3884039413, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType15($result);
    }));
}

/**
 * GetScrobbleConfig returns the current Last.fm scrobble configuration.
 * @returns {$CancellablePromise<$models.ScrobbleConfigJSON>}
 */
export function GetScrobbleConfig() {
    return $Call.ByID(3948527462).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType16($result);
    }));
}

/**
 * GetScrobbleQueueSize returns the number of scrobbles waiting for retry.
 * @returns {$CancellablePromise<number>}
 */
export function GetScrobbleQueueSize() {
    return $Call.ByID(4199289054);
}

/**
 * GetServerStatuses returns the online/offline status of all configured servers.
 * @returns {$CancellablePromise<$models.ServerStatusJSON[]>}
 */
export function GetServerStatuses() {
    return $Call.ByID(1839345631).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType18($result);
    }));
}

/**
 * GetServers returns all configured streaming servers.
 * @returns {$CancellablePromise<$models.ServerConfig[]>}
 */
export function GetServers() {
    return $Call.ByID(3711954650).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType20($result);
    }));
}

/**
 * GetSomaFMStations returns all SomaFM channels.
 * @returns {$CancellablePromise<$models.RadioStationJSON[]>}
 */
export function GetSomaFMStations() {
    return $Call.ByID(3406641218).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType13($result);
    }));
}

/**
 * GetTopAlbums returns the most-played albums for the given period.
 * @param {string} period
 * @param {number} limit
 * @returns {$CancellablePromise<$models.StatEntryJSON[]>}
 */
export function GetTopAlbums(period, limit) {
    return $Call.ByID(1740480677, period, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType22($result);
    }));
}

/**
 * GetTopArtists returns the most-played artists for the given period.
 * @param {string} period
 * @param {number} limit
 * @returns {$CancellablePromise<$models.StatEntryJSON[]>}
 */
export function GetTopArtists(period, limit) {
    return $Call.ByID(2628386383, period, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType22($result);
    }));
}

/**
 * GetTopClickedRadioStations returns the most clicked radio stations.
 * @param {number} limit
 * @returns {$CancellablePromise<$models.RadioStationJSON[]>}
 */
export function GetTopClickedRadioStations(limit) {
    return $Call.ByID(46869912, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType13($result);
    }));
}

/**
 * GetTopTracks returns the most-played tracks for the given period.
 * @param {string} period
 * @param {number} limit
 * @returns {$CancellablePromise<$models.StatEntryJSON[]>}
 */
export function GetTopTracks(period, limit) {
    return $Call.ByID(3437861925, period, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType22($result);
    }));
}

/**
 * GetTopVotedRadioStations returns the top voted radio stations.
 * @param {number} limit
 * @returns {$CancellablePromise<$models.RadioStationJSON[]>}
 */
export function GetTopVotedRadioStations(limit) {
    return $Call.ByID(1723581283, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType13($result);
    }));
}

/**
 * IsRadioFavourite checks if a station is in favourites.
 * @param {string} stationUUID
 * @returns {$CancellablePromise<boolean>}
 */
export function IsRadioFavourite(stationUUID) {
    return $Call.ByID(329793224, stationUUID);
}

/**
 * MoveTrackInPlaylist moves a track from one position to another.
 * @param {number} playlistID
 * @param {number} fromPos
 * @param {number} toPos
 * @returns {$CancellablePromise<void>}
 */
export function MoveTrackInPlaylist(playlistID, fromPos, toPos) {
    return $Call.ByID(465154155, playlistID, fromPos, toPos);
}

/**
 * RemoveRadioFavourite removes a radio station from favourites.
 * @param {string} stationUUID
 * @returns {$CancellablePromise<void>}
 */
export function RemoveRadioFavourite(stationUUID) {
    return $Call.ByID(876184048, stationUUID);
}

/**
 * RemoveTrackFromPlaylist removes a track from a playlist.
 * @param {number} playlistID
 * @param {number} trackID
 * @returns {$CancellablePromise<void>}
 */
export function RemoveTrackFromPlaylist(playlistID, trackID) {
    return $Call.ByID(970681807, playlistID, trackID);
}

/**
 * RenamePlaylist renames a playlist.
 * @param {number} id
 * @param {string} name
 * @returns {$CancellablePromise<void>}
 */
export function RenamePlaylist(id, name) {
    return $Call.ByID(3081673158, id, name);
}

/**
 * SaveScrobbleAPIKeys saves the Last.fm API key and secret.
 * @param {string} apiKey
 * @param {string} apiSecret
 * @returns {$CancellablePromise<void>}
 */
export function SaveScrobbleAPIKeys(apiKey, apiSecret) {
    return $Call.ByID(1590775235, apiKey, apiSecret);
}

/**
 * Search searches the library for tracks matching the query.
 * @param {string} query
 * @param {number} limit
 * @returns {$CancellablePromise<$models.SearchResult[]>}
 */
export function Search(query, limit) {
    return $Call.ByID(2206755262, query, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType24($result);
    }));
}

/**
 * SearchRadioStations searches for radio stations by name.
 * @param {string} query
 * @param {number} limit
 * @returns {$CancellablePromise<$models.RadioStationJSON[]>}
 */
export function SearchRadioStations(query, limit) {
    return $Call.ByID(1619368624, query, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType13($result);
    }));
}

/**
 * SetListenBrainzEnabled toggles ListenBrainz scrobbling on or off.
 * @param {boolean} enabled
 * @returns {$CancellablePromise<void>}
 */
export function SetListenBrainzEnabled(enabled) {
    return $Call.ByID(333272068, enabled);
}

/**
 * SetScrobbleEnabled toggles scrobbling on or off.
 * @param {boolean} enabled
 * @returns {$CancellablePromise<void>}
 */
export function SetScrobbleEnabled(enabled) {
    return $Call.ByID(22544365, enabled);
}

/**
 * StartLastFmAuth begins the Last.fm auth flow: requests a token and opens
 * the browser for user approval. Returns the token for use with CompleteLastFmAuth.
 * @returns {$CancellablePromise<string>}
 */
export function StartLastFmAuth() {
    return $Call.ByID(1558738173);
}

/**
 * SyncServers triggers an immediate sync of all server catalogs.
 * @returns {$CancellablePromise<void>}
 */
export function SyncServers() {
    return $Call.ByID(545152779);
}

/**
 * TestConnection tests connectivity to a streaming server without persisting it.
 * @param {$models.ServerConfig} cfg
 * @returns {$CancellablePromise<void>}
 */
export function TestConnection(cfg) {
    return $Call.ByID(3263505778, cfg);
}

/**
 * UpdateServer updates an existing streaming server configuration.
 * @param {$models.ServerConfig} cfg
 * @returns {$CancellablePromise<void>}
 */
export function UpdateServer(cfg) {
    return $Call.ByID(1667032524, cfg);
}

// Private type creation functions
const $$createType0 = $models.AlbumTrack.createFrom;
const $$createType1 = $Create.Array($$createType0);
const $$createType2 = $models.Album.createFrom;
const $$createType3 = $Create.Array($$createType2);
const $$createType4 = $models.ArtistInfoJSON.createFrom;
const $$createType5 = $models.ListenBrainzConfigJSON.createFrom;
const $$createType6 = $models.PlaylistTrack.createFrom;
const $$createType7 = $Create.Array($$createType6);
const $$createType8 = $models.Playlist.createFrom;
const $$createType9 = $Create.Array($$createType8);
const $$createType10 = $models.RadioFavouriteJSON.createFrom;
const $$createType11 = $Create.Array($$createType10);
const $$createType12 = $models.RadioStationJSON.createFrom;
const $$createType13 = $Create.Array($$createType12);
const $$createType14 = $models.RecentPlayJSON.createFrom;
const $$createType15 = $Create.Array($$createType14);
const $$createType16 = $models.ScrobbleConfigJSON.createFrom;
const $$createType17 = $models.ServerStatusJSON.createFrom;
const $$createType18 = $Create.Array($$createType17);
const $$createType19 = $models.ServerConfig.createFrom;
const $$createType20 = $Create.Array($$createType19);
const $$createType21 = $models.StatEntryJSON.createFrom;
const $$createType22 = $Create.Array($$createType21);
const $$createType23 = $models.SearchResult.createFrom;
const $$createType24 = $Create.Array($$createType23);
